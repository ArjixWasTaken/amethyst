import { array_shift, array_contains } from "std/array"

import { info, fatal } from "../../../lib/utils.ab"

fun show_help_and_exit() {
    echo "Usage: amethyst remove repo"
    echo ""
    echo "Options:"
    echo "  --help, -h    Show this help message and exit"
    exit(0)
}

pub fun cmd_deps_remove(arguments: [Text]): Null {
    fatal("stub for `amethyst remove [dep]`")
    exit(0)
}
